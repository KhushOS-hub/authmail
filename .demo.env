PORT = 3000
MONGO_URL= assign your mongodb url here
CORS_ORIGIN=*

REFRESH_TOKEN_SECRET= type the following command in terminal
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
REFRESH_TOKEN_EXPIRY=7d

ACCESS_TOKEN_SECRET=  type the following command in terminal
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_EXPIRY=15m

register to mailtrap.io
MAILTRAP_SMTP_HOST= 
MAILTRAP_SMTP_PORT=
MAILTRAP_SMTP_USER=
MAILTRAP_SMTP_PASS=